import React from 'react'
import { View } from 'react-native';
import { Icon } from 'native-base';

interface IExample {
    item: any;
}

export const Example: React.FC<IExample> = ({ item }) => {
 
    return <View />


};